# ============================================================
# 환경 변수 설정 예시
# 이 파일을 .env로 복사하여 사용하세요
# ============================================================

# ============================================================
# 애플리케이션 설정
# ============================================================
APP_NAME=onperm-hipster-timer-backend
APP_VERSION=1.0.0
DEBUG=false
LOG_LEVEL=INFO

# ============================================================
# 데이터베이스 설정
# ============================================================

# SQLite (기본값 - 개발/테스트용)
DATABASE_URL=sqlite:///./schedule.db

# PostgreSQL (프로덕션 권장)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# 커넥션 풀 설정 (PostgreSQL 등 외부 DB용)
POOL_SIZE=5
MAX_OVERFLOW=10
DB_POOL_PRE_PING=true
DB_POOL_RECYCLE=3600

# ============================================================
# OIDC 인증 설정
# ============================================================
OIDC_ENABLED=true
OIDC_ISSUER_URL=https://accounts.google.com
OIDC_AUDIENCE=your-client-id
# OIDC_DISCOVERY_URL=  # 기본: OIDC_ISSUER_URL/.well-known/openid-configuration
OIDC_JWKS_CACHE_TTL_SECONDS=3600

# ============================================================
# Rate Limit 설정
# ============================================================
RATE_LIMIT_ENABLED=true
RATE_LIMIT_DEFAULT_WINDOW=60
RATE_LIMIT_DEFAULT_REQUESTS=60

# ============================================================
# GraphQL 설정
# ============================================================
GRAPHQL_ENABLE_PLAYGROUND=true
GRAPHQL_ENABLE_INTROSPECTION=true

# ============================================================
# 외부 API 설정
# ============================================================
HOLIDAY_API_BASE_URL=http://apis.data.go.kr/B090041/openapi/service/SpcdeInfoService
HOLIDAY_API_SERVICE_KEY=your-service-key
